import styled from "styled-components"

const BreakArrowsContainer = styled.div`
    display: flex;
    p{
        margin: 0;
    }
    a{
        margin: 0 .5rem;
        cursor: pointer;
    }
`

const ClockTitle = styled.h1`
    margin: 0;
`

export {BreakArrowsContainer, ClockTitle}